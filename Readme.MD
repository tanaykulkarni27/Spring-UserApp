# Spring sample project
## How to run
Command to run the project
```bash
./gradlew clean 
./gradlew bootRun
```
Command to build the project
```bash
./gradlew clean build
```
## System requirements
### You can use any system with running java 25 and spring installed.
## Files to update 
update the credentials of the sql database in application.properties file
